<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <!-- Both themes descend from Theme.AppCompat for fingerprint sign-in
         (local_auth). On Android 8.1 and below the fingerprint prompt is an
         AppCompat dialog drawn with whichever of these themes the activity
         has at that moment, and it crashes the app if that theme is not
         AppCompat. The README names only LaunchTheme, but Flutter swaps to
         NormalTheme once the app is running, which is when the prompt shows.
         NoActionBar keeps the window as bare as NoTitleBar did. -->
    <style name="LaunchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
