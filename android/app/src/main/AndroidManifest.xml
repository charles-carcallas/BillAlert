<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      BillAlert talks to Supabase, so it needs this in EVERY build.

      Flutter's template declares INTERNET only in src/debug and src/profile,
      because the tooling needs it to reach a running app for hot reload. The
      main manifest is left to the developer - and a release APK built without
      this line cannot open a single socket. Sign-in fails with correct
      credentials, and every other build works, which makes it look like
      anything but a permission.

      Do not move this into a build-type manifest.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--
      Due-date reminders are scheduled with Android's alarm service, and a
      restart clears every alarm. This lets flutter_local_notifications put
      them back after the phone boots. POST_NOTIFICATIONS itself comes from the
      plugin's own manifest.
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!--
      Urgent due-date alerts fill the screen on Android 14 and later. Android
      reserves that for alarm and calling apps unless the household allows it,
      which Profile asks for when the switch is turned on. The activity is NOT
      marked showWhenLocked here: MainActivity turns that on only for the
      alert that woke the phone, and asks for the phone's unlock straight away.
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>

    <application
        android:label="billalert"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Show scheduled due-date reminders, and reschedule them after a
             restart or an app update. Without these, zonedSchedule succeeds
             and the reminder never appears. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
